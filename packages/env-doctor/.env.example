# env-doctor does not currently read any environment variables itself -
# it only parses the .env / .env.example files you point it at.
# This file exists to match the monorepo's package layout convention.
